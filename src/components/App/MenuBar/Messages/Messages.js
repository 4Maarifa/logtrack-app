import React from 'react';

/**
 * Component: Messages
 * Used by everyone to send private messages
 * 
 * TODO: Messages
 */
const Messages = ({ rt }) => {

  /**
   * RENDER
   */
  return <div className="MenuBar-content Messages">
    
  </div>;
};

export default Messages;
